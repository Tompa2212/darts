export const privateRoutes = ['/settings', '/teams'];

export const authRoutes = [
  '/login',
  '/register',
  '/error',
  '/reset',
  '/new-password'
];

export const apiAuthPrefix = '/api/auth';

export const DEFAULT_LOGIN_REDIRECT = '/play';
